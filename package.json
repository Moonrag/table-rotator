{
  "name": "csv-table-rotator",
  "version": "0.1.0",
  "description": "CSV rotation utility library",
  "main": "index.js",
  "types": "./build/src/types.d.ts",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc --project tsconfig.build.json",
    "test": "jest"
  },
  "directories": {
    "lib": "src"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.13.0",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "csv-stream": "^0.2.0",
    "fast-csv": "^4.3.6"
  }
}
